## Provider.ts

The provider is used in the ProviderView.

The loginOAuth function is executed on click on the button to login with the
selected provider (Box, DropBox...).

## Request client

The hostname is get from the uppy instance options.
